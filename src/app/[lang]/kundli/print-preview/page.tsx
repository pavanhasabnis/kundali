import PrintPreviewClient from "./preview-client";

export const metadata = {
  title: "Kundli Print Preview — Layout Mockup",
  robots: { index: false, follow: false },
};

export default function Page() {
  return <PrintPreviewClient />;
}
